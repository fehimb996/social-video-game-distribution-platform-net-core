@model IEnumerable<DrustvenaPlatformaVideoIgara.Models.Developer>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DeveloperName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DeveloperName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DeveloperId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DeveloperId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DeveloperId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
