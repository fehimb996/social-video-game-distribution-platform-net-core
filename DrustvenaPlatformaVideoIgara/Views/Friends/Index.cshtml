@model IEnumerable<DrustvenaPlatformaVideoIgara.Models.Friend>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserId1Navigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserId2Navigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserId1Navigation.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserId2Navigation.UserId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FriendId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FriendId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FriendId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
